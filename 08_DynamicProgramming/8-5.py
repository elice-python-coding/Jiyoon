'''
[실전문제 2번] 1로 만들기
정수 x가 주어질 때 정수 x에 사용할 수 있는 연산은 다음과 같이 4가지이다.
    a. x가 5로 나누어떨어지면, 5로 나눈다.
    b. x가 3으로 나누어떨어지면, 3으로 나눈다.
    c. x가 2로 나누어떨어지면, 2로 나눈다.
    d. x에서 1을 뺀다.
정수 x가 주어졌을 때, 연산 4개를 적절히 사용해서 1을 만드려고 한다. 연산을 사용하는 횟수의 최솟값을 출력하시오.
'''

x = int(input())

d = [0] * 30001

for i in range(2, x+1):     # 26을 예로 들면
    d[i] = d[i-1] + 1       # d[26] = d[25] + 1
    if i % 2 == 0:
        d[i] = min(d[i], d[i//2] + 1)   # d[26] = min(d[26], d[13] + 1)
    if i % 3 == 0:
        d[i] = min(d[i], d[i//3] + 1)
    if i % 5 == 0:
        d[i] = min(d[i], d[i//5] + 1)

print(d[x])